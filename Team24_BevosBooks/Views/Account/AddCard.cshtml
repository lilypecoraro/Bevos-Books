@model Team24_BevosBooks.Models.ViewModels.CardViewModel

@{
    ViewData["Title"] = "Add Credit Card";
}

<h2 class="mt-4 mb-3">Add a Credit Card</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="AddCard" method="post">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <!-- CARD TYPE -->
            <div class="form-group mb-3">
                <label asp-for="CardType"></label>

                <!-- Dropdown auto-populates Visa, MasterCard, Discover, AmericanExpress -->
                <select asp-for="CardType"
                        asp-items="Html.GetEnumSelectList<Team24_BevosBooks.Models.Card.CardTypes>()"
                        class="form-control">
                </select>

                <span asp-validation-for="CardType" class="text-danger"></span>
            </div>

            <!-- CARD NUMBER -->
            <div class="form-group mb-3">
                <label asp-for="CardNumber"></label>
                <input asp-for="CardNumber" class="form-control" />
                <span asp-validation-for="CardNumber" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary w-100">Add Card</button>
        </form>
    </div>
</div>